<div class="progress" style="height: 20px;">
  <div class="progress-bar"
    role="progressbar"
    style="width: <%= 100 * c.current_amount_cents/c.target_amount.cents %>%;"
    aria-valuenow="<%= c.current_amount_cents %>"
    aria-valuemin="0"
    aria-valuemax="<%= c.target_amount.cents %>">
  </div>
</div>
<div class="row">
  <div class="col">
    <h5 class="mt-1">
      S<%= Money.new(c.current_amount).format(
        no_cents: true,
        symbol_before_without_space: true
      ) %> Raised
    </h5>
    <p>
      out of <%= c.target_amount.format(
        no_cents: true,
        symbol_before_without_space: true
      ) %> Goal
      (<%= (c.target_amount - Money.new(c.current_amount)).format(
        no_cents: true,
        symbol_before_without_space: true
      ) %> left)
    </p>
  </div>
</div>
